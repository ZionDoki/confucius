{
  "name": "@confucius/chrome-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Confucius Chrome side panel — ACP client and browser context",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "typecheck": "node --check workspace-app.js && node --check sidepanel.js && node --check content-script.js && node --check background.js"
  }
}
